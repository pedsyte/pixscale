[Unit]
Description=PixScale API (image resize & AI upscale)
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/pixscale/backend
ExecStart=/usr/bin/python3.11 -m uvicorn main:app --host 127.0.0.1 --port 8093
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
